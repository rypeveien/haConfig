- id: '1637565436384'
  alias: Kompressor av/på og offline
  description: ''
  trigger:
  - platform: template
    value_template: >
      {% set hour = now().hour %}
      {% set switch = states('switch.plugg_kompressor') %}
      {{ (hour >= 10 and hour < 21 and  switch == 'off') 
          or (hour >= 21 and  switch == 'on') }}
  action:
    - service: switch.toggle
      target:
        device_id: 211bf8bc4c53b243d144322c49dcec6c
  mode: single
